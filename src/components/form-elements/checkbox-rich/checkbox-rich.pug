include ../checkbox/checkbox

mixin checkbox-rich(id, text, checked=false)
    .checkbox-rich
        .checkbox-rich__button
            +checkbox-button(id, text, checked)
        .checkbox-rich__description
